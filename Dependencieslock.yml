# Create a folder under the drive root
$ mkdir actions-runner; cd actions-runnerCopied!# Download the latest runner package
$ Invoke-WebRequest -Uri https://github.com/actions/runner/releases/download/v2.332.0/actions-runner-win-x64-2.332.0.zip -OutFile actions-runner-win-x64-2.332.0.zip# Optional: Validate the hash
$ if((Get-FileHash -Path actions-runner-win-x64-2.332.0.zip -Algorithm SHA256).Hash.ToUpper() -ne '83e56e05b21eb58c9697f82e52c53b30867335ff039cd5d44d1a1a24d2149f4b'.ToUpper()){ throw 'Computed checksum did not match' }Copied!# Extract the installer
$ Add-Type -AssemblyName System.IO.Compression.FileSystem ; [System.IO.Compression.ZipFile]::ExtractToDirectory("$PWD/actions-runner-win-x64-2.332.0.zip", "$PWD")Copied!
Configure
# Create the runner and start the configuration experience
$ ./config.cmd --url https://github.com/tijuks/Global-football-review--c953490b --token BE56E3O4HHFUSA44PSQL4Y3JVBA6Q# Run it!
$ ./run.cmd
Using your self-hosted runner
# Use this YAML in your workflow file for each job
runs-on: self-hosted
